// 変数名.pop()	配列の最後のデータを削除する
// 変数名.push(データ)	配列の最後にデータを追加する
// 変数名.shift()	配列の最初のデータを削除する
// 変数名.unshift(データ)	配列の最初にデータを追加する
//変数.splice(start, deleteCount, データ1,データ2, ... データX) インデックス位置を指定してデータを削除する（splice）


let theme = ['XAMPP', 'jQuery', 'Javascript', 'Security'];

// theme.pop();
// console.log(theme);

// theme.push('PHP');
// console.log(theme);

// theme.shift();
// console.log(theme);

// theme.unshift('Sass');
// console.log(theme);

theme.splice(1, 2,'Vue','Bootstrap','Ruby');
console.log(theme);


